/*Varias lineas
 de comentarios
 en el codigo*/
var int num = 30
var chars num = true

 function int sumaSimple(int b){
	var int b
	return num + b
 }
 
var bool esFlag = true + 2

/*Varias lineas
 de comentarios
 en cualquier parte del codigo*/
 
while("f"){
	write("hola")
}
if(esFlag && true) write("mundo")

auxiliar = 303330+sumaSimple(num)+2

if(num>Z) yes = "Verdad"

prompt(num)

/*Varias lineas
 de comentarios
 en el codigo*/